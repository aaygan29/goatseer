# Synthesis: mathematical + computational-neuroscience anchors

The math side of NEUROSPINE now rests on four load-bearing pillars.
Each pillar has its external anchor(s), maps to concrete `neurospine`
package modules, and is verified by ADR-003-style identity tests.

## Pillar 1: Riemannian geometry on the SPD manifold

External anchors:

- **`pubmed-22010143` Barachant et al. 2012**: foundational
  Riemannian-BCI paper. AIRM distance + minimum-distance-to-mean +
  tangent-space LDA on covariance matrices. Empirical validation on
  BCI Competition IV dataset IIa.
- Bhatia, "Positive Definite Matrices" (Princeton, 2007): theory
  reference. Textbook citation added to `references.bib`.

NEUROSPINE binding: `manifold.py` implements AIRM primitives
(`airm_distance`, `airm_geodesic`, `airm_log_map`, `airm_exp_map`,
`airm_parallel_transport`, `airm_frechet_mean`) whose identities
(symmetry, affine invariance, geodesic endpoints, log-exp duality,
parallel-transport-preserves-inner-product, Frechet gradient
vanishes at the mean) are all covered by `test_manifold.py`. Gate
G14 (manifold correctness) requires these tests to pass before any
SPD-family claim ships.

## Pillar 2: Neural manifold theory and low-dimensional cortical dynamics

External anchors:

- **`pubmed-31320220` Sohn, Narain, Meirhaeghe, Jazayeri 2019**:
  Bayesian computation through a low-dim curved cortical manifold in
  monkey frontal cortex; RNN reproduces the effect; causal
  perturbations implicate the manifold.
- `pubmed-40502061` Schmutz et al. 2025: analytically solvable RNN
  with high-dim activity generated by low-dim latents; NCE
  diagnostic.
- `pubmed-42599379` O'Reilly-Shah + Selvitella 2026: Whitney/Takens
  embedding-theorem bounds on internal manifold dimension.
- `pubmed-38452763` Manley et al. 2024: unbounded dimensionality
  scaling with neuron count; recording-scale caveat.
- `pubmed-41570814` Han + Bonner 2026: high-dim naturalistic
  individual differences on a visual manifold.

NEUROSPINE binding: `LatentState` in `manifold.py` carries a `family`
tag distinguishing SPD, Grassmann, and learned-latent manifolds, so
per-subject decoders live on the geometry their data actually
respects. The topological signature layer (Pillar 3) captures the
Sohn-style curved-manifold structure without committing to a
specific parametric form.

## Pillar 3: Persistent homology and topological invariants

External anchors:

- **`arxiv-2210.09092`** Dynamic TDA of functional human brain
  networks.
- **`arxiv-2512.08637`** Persistent homology pipeline for neural
  spike-train data.
- `arxiv-2509.14634` Interpretable higher-order topological features
  across scales.
- `arxiv-2406.15505` Integral Betti signature confirming hyperbolic
  geometry of brain networks.
- Edelsbrunner + Harer, "Computational Topology" (AMS, 2010).
- Chazal, de Silva, Glisse, Oudot, "The Structure and Stability of
  Persistence Modules" (Springer, 2016).

NEUROSPINE binding: `topology.py` implements Vietoris-Rips
persistent homology (H0 via union-find, H1 via boundary reduction),
Betti curves over filtration scale, and a Lipschitz-bounded
bottleneck distance. Verified against synthetic point clouds with
known Betti numbers (unit circle: b0 = 1, b1 = 1; two circles:
b0 = 2 dominant merge; scattered cloud: no persistent H1) in
`test_topology.py`.

The topological signature is the primary A1 replicability
invariant: two sessions from the same subject can differ pointwise
and still have bottleneck-close persistence diagrams.

## Pillar 4: Purpose-constrained intervention via geodesic alignment

External anchors:

- **`pubmed-33626312` Friston, Da Costa, Hafner, Hesp, Parr 2021**
 : sophisticated active inference; expected free energy as a
  formal-mathematical objective for scoring counterfactual actions.
- **`pubmed-42618509` Miller, Brincat, Roy 2026**: wave-mediated
  top-down control as the mesoscale substrate for the intervention
  channels.
- **`pubmed-22010143` Barachant et al. 2012**: tangent-space
  projection at a point is the technical basis for computing
  channel pushforwards.

NEUROSPINE binding: `intervention.py` computes the AIRM geodesic
tangent from current to target state, projects each channel's
pushforward into the same tangent space, and ranks channels by the
affine-invariant cosine alignment. Purpose is required and drawn
from the ADR-managed `PURPOSE_REGISTRY`. Safety margin = AIRM
distance from the geodesic midpoint to a declared out-of-scope
subregion.

Deferred: replacing AIRM cosine alignment with expected-free-energy
scoring (ADR-009, planned). The current implementation is a
first-order approximation; EFE is the correct large-step objective.

## Cross-cutting: information geometry, optimal transport, Bayesian brain

- **`pubmed-36495600` Gerber et al. 2022**: unbalanced optimal
  transport as a metric on tissue-distribution manifolds; candidate
  for a future `LatentState` family via ADR-010.
- The Sohn et al. Bayesian-manifold result and Friston sophisticated-
  inference paper together frame NEUROSPINE's per-subject reads as
  an approximation to the subject's own Bayesian posterior update
  trajectory, giving a formal-mathematical target beyond
  pattern-matching.

## What this synthesis rules out

- **Flat-vector claims about SPD data.** Any covariance-derived
  quantity in NEUROSPINE that ignores the AIRM manifold structure
  is a G14 fail. `perception.py` (linear baseline from an earlier
  tick) is retained as a comparison baseline but is not used to
  ship gate-cleared claims.
- **Per-timepoint-only replicability.** A1 replicability targets
  bottleneck-distance similarity between per-subject persistence
  diagrams, not per-timepoint prediction identity.
- **Intervention without purpose.** `PurposeNotRegisteredError` is
  raised at construction time; no code path lets an intervention
  ship without an ADR-approved purpose.

## What this synthesis does not yet cover (queued)

- Neural field theory (Wilson-Cowan, Amari) as a mesoscale model
  of the wave-mediated coordination in `pubmed-42618509`. Pubmed
  search returned zero exact hits; needs an arxiv or bioRxiv-side
  search in the next tick.
- Information geometry (Fisher-Rao metric on parametric probability
  distributions) as a metric on the LearnedLatent family.
- Free-energy-based scoring in `intervention.py` (ADR-009 target).
- Neural mass models and dynamic causal modeling (DCM) as
  candidates for the `brain_state` estimator once ADR-007 is
  adopted.

## Attribution

According to PubMed for all `pubmed-*` slugs. DOI links present on
every note. External textbook citations added to `references.bib`
as `bhatia-2007`, `edelsbrunner-harer-2010`, `chazal-desilva-2016`.
