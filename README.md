# NEW_REPO

This repository now includes a **consent-based thought evaluator**.

It does **not** read minds. Instead, it evaluates sentiment from text that a person voluntarily provides.

## Usage

```bash
python thought_evaluator.py --consent --text "I feel happy and hopeful"
```

If `--consent` is omitted, evaluation is blocked.
